name: Build WristCursor APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Clone WristCursor
        run: |
          git clone https://github.com/varaddorle007/wristcursor.git
          cd wristcursor
          git log -1 --oneline

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install Android SDK components
        run: |
          yes | sdkmanager --licenses || true
          sdkmanager \
            "platforms;android-34" \
            "build-tools;34.0.0" \
            "ndk;26.1.10909125" \
            "cmake;3.22.1"

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Build APK
        run: |
          cd wristcursor
          gradle --no-daemon assembleDebug

      - name: Check APK
        run: |
          ls -lh wristcursor/app/build/outputs/apk/debug/

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: WristCursor-X2Mini-debug-apk
          path: wristcursor/app/build/outputs/apk/debug/app-debug.apk
